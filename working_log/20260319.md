# 작업 로그 - 2026.03.19

## 타이머 화면 UI 구현
- `lib/screens/timer_screen.dart` 생성
  - 라운드 표시 (현재 / 총 라운드)
  - 원형 타이머 (시간 표시)
  - 시작/일시정지 토글 버튼 (타이머 원 안)
  - 리셋 버튼 (타이머 원 아래)
  - 운동시간 설정 버튼
  - 프리셋 불러오기 버튼
- `lib/screens/main_screen.dart` 수정 — TimerScreen 연결
- ARB 파일 4개에 타이머 관련 문자열 추가 (round, start, pause, reset, timerSetting, loadPreset)
- UI만 구현, 기능 로직은 미구현

## 텍스트 스타일 중앙 관리 적용
- `main.dart`에 `textTheme` 정의 (displayLarge, titleLarge, bodyMedium, labelMedium)
- `timer_screen.dart`, `home_screen.dart`, `common_bottom_nav.dart`에서 직접 fontSize 대신 Theme 참조로 변경

## 운동시간 설정 팝업 구현
- `timer_screen.dart`에 설정 다이얼로그 추가
  - 라운드 수 입력 (기본값: 12)
  - 운동시간 분/초 입력 (기본값: 3분 0초)
  - 휴식시간 분/초 입력 (기본값: 0분 30초)
  - 확인 시 타이머 화면에 값 반영, 취소 시 변경 없음
  - 숫자만 입력 가능 (FilteringTextInputFormatter)
- 리셋 버튼에 라운드 초기화 기능 추가
- 타이머 시간 표시를 설정값 기반으로 변경
- ARB 파일 4개에 문자열 추가 (roundLabel, exerciseTime, restTime, minute, second, cancel, confirm)

## 프리셋 저장하기 버튼 추가
- `timer_screen.dart`에 "프리셋 저장하기" 버튼 추가 (운동시간 설정 아래, 프리셋 불러오기 위)
- ARB 파일 4개에 `savePreset` 문자열 추가
- 저장 기능 로직은 미구현 (UI만 배치)

## 타이머 기능 구현
- `timer_screen.dart` 전면 리팩토링
  - `TimerPhase` enum 추가 (idle, ready, exercise, rest, finished)
  - 타이머 흐름: 시작 → 대기(30초) → 운동(3:00) → 휴식(0:30) → 반복 → 마지막 라운드 운동 후 자동 종료
  - 상태 텍스트 표시 (대기 중 / 운동 중 / 휴식 중 / 완료)
  - 상태별 원형 테두리 색상 변경 (대기: 주황, 운동: 녹색, 휴식: 파랑)
  - 일시정지/재개 기능
  - 타이머 실행 중 설정/프리셋 버튼 비활성화
- 설정 팝업에 대기시간 항목 추가 (기본값: 0분 30초)
- ARB 파일 4개에 문자열 추가 (waitTime, statusReady, statusExercise, statusRest, statusFinished)

## 원형 프로그레스 애니메이션 추가
- `timer_screen.dart`에 `_TimerCirclePainter` (CustomPainter) 추가
  - 12시 방향에서 시작, 시간 경과에 따라 원이 채워지는 애니메이션
  - 배경 원(회색) + 진행 원(상태별 색상)
  - 각 phase 총 시간 대비 경과 비율로 진행도 계산
